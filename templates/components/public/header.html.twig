<header class="bg-white border-b border-gray-200" data-controller="mobile-menu">
    <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
        <a href="{{ path('app_home') }}" class="flex items-center gap-3">
            <img src="{{ asset('images/matu-loire-logo-couleur-sans-fond2-11.png') }}"
                 alt="Les Mathu'Loire"
                 class="h-10 sm:h-12">
        </a>

        <nav class="hidden md:flex items-center gap-8">
            <a href="{{ path('app_home') }}" class="text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium">Accueil</a>
            <a href="{{ path('app_reservation_index') }}" class="text-gray-600 hover:text-gray-900 transition-colors text-sm font-medium">Billetterie</a>
        </nav>

        <button data-action="mobile-menu#toggle" class="md:hidden p-2 text-gray-600 hover:text-gray-900">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
        </button>
    </div>

    <div data-mobile-menu-target="menu" class="hidden md:hidden border-t border-gray-200">
        <div class="px-4 py-3 space-y-2">
            <a href="{{ path('app_home') }}" class="block text-gray-600 hover:text-gray-900 text-sm font-medium py-1">Accueil</a>
            <a href="{{ path('app_reservation_index') }}" class="block text-gray-600 hover:text-gray-900 text-sm font-medium py-1">Billetterie</a>
        </div>
    </div>
</header>
